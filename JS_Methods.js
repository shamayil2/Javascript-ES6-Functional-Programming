string.toLowerCase() // to lower case entire string
string.toUpperCase() // - to convert to upper case entire string
const currTime = new Date().toLocaleTimeString() // to convert to HH:MM:SS format
let formattedDate = new Date().toLocaleDateString() // to convert to MM/DD/YYYY format
tempInCelsius = (tempInFahrenheit - 32) * (5 / 9) // to convert temperature in Fahrehnheit to Celsius
const randomNum = Math.floor(Math.random() * 50) + 1; // to generate random number bw 1 and 30
Math.PI // Value of pi : 3.14